# keil/ workflow

Перенесён в **[`tests/src_c/keil/README.md`](../tests/src_c/keil/README.md)**.

Эталоны, `_prefix/`, манифест — **только** под `tests/src_c/keil/`.  
Склеивать prefix + body **вручную** в редакторе. PowerShell-скриптов нет.
